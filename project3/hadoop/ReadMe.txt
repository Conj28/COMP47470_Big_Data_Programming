all hadoop files are self-contained and do not require the output from another job. the code in q3,4 & 5
may be repeated in creating the adjacency list but this was done to ensure that 
no dependencies between questions were created. all files are run the same way as seen in the tutorials.
